/* <begin connected objects> */
/*
This file is auto generated by pgrx.

The ordering of items is not stable, it is driven by a dependency graph.
*/
/* </end connected objects> */

/* <begin connected objects> */
-- src/schema.rs:3
-- bootstrap

CREATE SCHEMA epanet;

CREATE TABLE epanet.networks (
    id          SERIAL PRIMARY KEY,
    name        TEXT NOT NULL,
    imported_at TIMESTAMPTZ NOT NULL DEFAULT now(),
    srid        INT NOT NULL,
    inp_text    TEXT NOT NULL
);

CREATE INDEX epanet_networks_name ON epanet.networks(name);

CREATE TABLE epanet.junctions (
    network_id  INT NOT NULL REFERENCES epanet.networks(id) ON DELETE CASCADE,
    name        TEXT NOT NULL,
    elevation   FLOAT8 NOT NULL,
    demand      FLOAT8 NOT NULL,
    pattern     TEXT,
    geom        geometry(Point),
    PRIMARY KEY (network_id, name)
);

CREATE TABLE epanet.reservoirs (
    network_id  INT NOT NULL REFERENCES epanet.networks(id) ON DELETE CASCADE,
    name        TEXT NOT NULL,
    head        FLOAT8 NOT NULL,
    pattern     TEXT,
    geom        geometry(Point),
    PRIMARY KEY (network_id, name)
);

CREATE TABLE epanet.tanks (
    network_id   INT NOT NULL REFERENCES epanet.networks(id) ON DELETE CASCADE,
    name         TEXT NOT NULL,
    elevation    FLOAT8 NOT NULL,
    init_level   FLOAT8 NOT NULL,
    min_level    FLOAT8 NOT NULL,
    max_level    FLOAT8 NOT NULL,
    diameter     FLOAT8 NOT NULL,
    min_volume   FLOAT8 NOT NULL,
    volume_curve TEXT,
    overflow     TEXT,
    geom         geometry(Point),
    PRIMARY KEY (network_id, name)
);

CREATE TABLE epanet.pipes (
    network_id  INT NOT NULL REFERENCES epanet.networks(id) ON DELETE CASCADE,
    name        TEXT NOT NULL,
    node1       TEXT NOT NULL,
    node2       TEXT NOT NULL,
    length      FLOAT8 NOT NULL,
    diameter    FLOAT8 NOT NULL,
    roughness   FLOAT8 NOT NULL,
    minor_loss  FLOAT8 NOT NULL,
    status      TEXT NOT NULL,
    geom        geometry(LineString),
    PRIMARY KEY (network_id, name)
);

CREATE TABLE epanet.pumps (
    network_id  INT NOT NULL REFERENCES epanet.networks(id) ON DELETE CASCADE,
    name        TEXT NOT NULL,
    node1       TEXT NOT NULL,
    node2       TEXT NOT NULL,
    pump_type   TEXT,
    head_curve  TEXT,
    power       FLOAT8,
    speed       FLOAT8,
    pattern     TEXT,
    geom        geometry(LineString),
    PRIMARY KEY (network_id, name)
);

CREATE TABLE epanet.valves (
    network_id  INT NOT NULL REFERENCES epanet.networks(id) ON DELETE CASCADE,
    name        TEXT NOT NULL,
    node1       TEXT NOT NULL,
    node2       TEXT NOT NULL,
    diameter    FLOAT8 NOT NULL,
    valve_type  TEXT NOT NULL,
    setting     TEXT NOT NULL,
    minor_loss  FLOAT8 NOT NULL,
    geom        geometry(LineString),
    PRIMARY KEY (network_id, name)
);

CREATE TABLE epanet.coordinates (
    network_id  INT NOT NULL REFERENCES epanet.networks(id) ON DELETE CASCADE,
    node_id     TEXT NOT NULL,
    x           FLOAT8 NOT NULL,
    y           FLOAT8 NOT NULL,
    PRIMARY KEY (network_id, node_id)
);

CREATE TABLE epanet.vertices (
    network_id  INT NOT NULL REFERENCES epanet.networks(id) ON DELETE CASCADE,
    link_id     TEXT NOT NULL,
    idx         INT NOT NULL,
    x           FLOAT8 NOT NULL,
    y           FLOAT8 NOT NULL,
    PRIMARY KEY (network_id, link_id, idx)
);

CREATE VIEW epanet.nodes AS
    SELECT network_id, name AS node_id, 'junction'::text AS node_type, elevation, geom
      FROM epanet.junctions
    UNION ALL
    SELECT network_id, name, 'tank',      elevation, geom FROM epanet.tanks
    UNION ALL
    SELECT network_id, name, 'reservoir', head,      geom FROM epanet.reservoirs;

CREATE TABLE epanet.simulation_runs (
    id         SERIAL PRIMARY KEY,
    network_id INT NOT NULL REFERENCES epanet.networks(id) ON DELETE CASCADE,
    ran_at     TIMESTAMPTZ NOT NULL DEFAULT now(),
    n_steps    INT NOT NULL
);

CREATE TABLE epanet.node_results (
    run_id   INT NOT NULL REFERENCES epanet.simulation_runs(id) ON DELETE CASCADE,
    step     INT NOT NULL,
    node_id  TEXT NOT NULL,
    head     DOUBLE PRECISION,
    pressure DOUBLE PRECISION,
    demand   DOUBLE PRECISION,
    PRIMARY KEY (run_id, step, node_id)
);

CREATE INDEX node_results_run ON epanet.node_results(run_id);

CREATE TABLE epanet.link_results (
    run_id    INT NOT NULL REFERENCES epanet.simulation_runs(id) ON DELETE CASCADE,
    step      INT NOT NULL,
    link_id   TEXT NOT NULL,
    flow      DOUBLE PRECISION,
    velocity  DOUBLE PRECISION,
    headloss  DOUBLE PRECISION,
    PRIMARY KEY (run_id, step, link_id)
);

CREATE INDEX link_results_run ON epanet.link_results(run_id);

CREATE INDEX junctions_geom  ON epanet.junctions  USING GIST (geom);
CREATE INDEX reservoirs_geom ON epanet.reservoirs USING GIST (geom);
CREATE INDEX tanks_geom      ON epanet.tanks      USING GIST (geom);
CREATE INDEX pipes_geom      ON epanet.pipes      USING GIST (geom);
CREATE INDEX pumps_geom      ON epanet.pumps      USING GIST (geom);
CREATE INDEX valves_geom     ON epanet.valves     USING GIST (geom);
/* </end connected objects> */

/* <begin connected objects> */
-- src/lib.rs:459
-- pg_epanet::epanet_coordinates
CREATE  FUNCTION "epanet_coordinates"(
	"inp_text" TEXT /* & str */
) RETURNS TABLE (
	"node_id" TEXT,  /* String */
	"x" double precision,  /* f64 */
	"y" double precision  /* f64 */
)
STRICT
LANGUAGE c /* Rust */
AS 'MODULE_PATHNAME', 'epanet_coordinates_wrapper';
/* </end connected objects> */

/* <begin connected objects> */
-- src/lib.rs:198
-- pg_epanet::epanet_delete
CREATE  FUNCTION "epanet_delete"(
	"network_id" INT /* i32 */
) RETURNS bool /* bool */
STRICT
LANGUAGE c /* Rust */
AS 'MODULE_PATHNAME', 'epanet_delete_wrapper';
/* </end connected objects> */

/* <begin connected objects> */
-- src/lib.rs:89
-- pg_epanet::epanet_import
CREATE  FUNCTION "epanet_import"(
	"network_name" TEXT, /* & str */
	"inp_text" TEXT, /* & str */
	"srid" INT DEFAULT 5367 /* i32 */
) RETURNS INT /* i32 */
STRICT
LANGUAGE c /* Rust */
AS 'MODULE_PATHNAME', 'epanet_import_wrapper';
/* </end connected objects> */

/* <begin connected objects> */
-- src/lib.rs:391
-- pg_epanet::epanet_junctions
CREATE  FUNCTION "epanet_junctions"(
	"inp_text" TEXT /* & str */
) RETURNS TABLE (
	"name" TEXT,  /* String */
	"elevation" double precision,  /* f64 */
	"demand" double precision,  /* f64 */
	"pattern" TEXT  /* Option < String > */
)
STRICT
LANGUAGE c /* Rust */
AS 'MODULE_PATHNAME', 'epanet_junctions_wrapper';
/* </end connected objects> */

/* <begin connected objects> */
-- src/lib.rs:422
-- pg_epanet::epanet_pipes
CREATE  FUNCTION "epanet_pipes"(
	"inp_text" TEXT /* & str */
) RETURNS TABLE (
	"name" TEXT,  /* String */
	"node1" TEXT,  /* String */
	"node2" TEXT,  /* String */
	"length" double precision,  /* f64 */
	"diameter" double precision,  /* f64 */
	"roughness" double precision,  /* f64 */
	"minor_loss" double precision,  /* f64 */
	"status" TEXT  /* String */
)
STRICT
LANGUAGE c /* Rust */
AS 'MODULE_PATHNAME', 'epanet_pipes_wrapper';
/* </end connected objects> */

/* <begin connected objects> */
-- src/lib.rs:502
-- pg_epanet::epanet_pumps
CREATE  FUNCTION "epanet_pumps"(
	"inp_text" TEXT /* & str */
) RETURNS TABLE (
	"name" TEXT,  /* String */
	"node1" TEXT,  /* String */
	"node2" TEXT,  /* String */
	"pump_type" TEXT,  /* Option < String > */
	"head_curve" TEXT,  /* Option < String > */
	"power" double precision,  /* Option < f64 > */
	"speed" double precision,  /* Option < f64 > */
	"pattern" TEXT  /* Option < String > */
)
STRICT
LANGUAGE c /* Rust */
AS 'MODULE_PATHNAME', 'epanet_pumps_wrapper';
/* </end connected objects> */

/* <begin connected objects> */
-- src/lib.rs:15
-- pg_epanet::epanet_reservoirs
CREATE  FUNCTION "epanet_reservoirs"(
	"inp_text" TEXT /* & str */
) RETURNS TABLE (
	"name" TEXT,  /* String */
	"head" double precision,  /* f64 */
	"pattern" TEXT  /* Option < String > */
)
STRICT
LANGUAGE c /* Rust */
AS 'MODULE_PATHNAME', 'epanet_reservoirs_wrapper';
/* </end connected objects> */

/* <begin connected objects> */
-- src/lib.rs:209
-- pg_epanet::epanet_simulate
CREATE  FUNCTION "epanet_simulate"(
	"network_id" INT /* i32 */
) RETURNS INT /* i32 */
STRICT
LANGUAGE c /* Rust */
AS 'MODULE_PATHNAME', 'epanet_simulate_wrapper';
/* </end connected objects> */

/* <begin connected objects> */
-- src/lib.rs:37
-- pg_epanet::epanet_tanks
CREATE  FUNCTION "epanet_tanks"(
	"inp_text" TEXT /* & str */
) RETURNS TABLE (
	"name" TEXT,  /* String */
	"elevation" double precision,  /* f64 */
	"init_level" double precision,  /* f64 */
	"min_level" double precision,  /* f64 */
	"max_level" double precision,  /* f64 */
	"diameter" double precision,  /* f64 */
	"min_volume" double precision,  /* f64 */
	"volume_curve" TEXT,  /* Option < String > */
	"overflow" TEXT  /* Option < String > */
)
STRICT
LANGUAGE c /* Rust */
AS 'MODULE_PATHNAME', 'epanet_tanks_wrapper';
/* </end connected objects> */

/* <begin connected objects> */
-- src/lib.rs:565
-- pg_epanet::epanet_valves
CREATE  FUNCTION "epanet_valves"(
	"inp_text" TEXT /* & str */
) RETURNS TABLE (
	"name" TEXT,  /* String */
	"node1" TEXT,  /* String */
	"node2" TEXT,  /* String */
	"diameter" double precision,  /* f64 */
	"valve_type" TEXT,  /* String */
	"setting" TEXT,  /* String */
	"minor_loss" double precision  /* f64 */
)
STRICT
LANGUAGE c /* Rust */
AS 'MODULE_PATHNAME', 'epanet_valves_wrapper';
/* </end connected objects> */

/* <begin connected objects> */
-- src/lib.rs:480
-- pg_epanet::epanet_vertices
CREATE  FUNCTION "epanet_vertices"(
	"inp_text" TEXT /* & str */
) RETURNS TABLE (
	"link_id" TEXT,  /* String */
	"x" double precision,  /* f64 */
	"y" double precision  /* f64 */
)
STRICT
LANGUAGE c /* Rust */
AS 'MODULE_PATHNAME', 'epanet_vertices_wrapper';
/* </end connected objects> */

/* <begin connected objects> */
-- src/lib.rs:9
-- pg_epanet::hello_pg_epanet
CREATE  FUNCTION "hello_pg_epanet"() RETURNS TEXT /* & '_ str */
STRICT
LANGUAGE c /* Rust */
AS 'MODULE_PATHNAME', 'hello_pg_epanet_wrapper';
/* </end connected objects> */

